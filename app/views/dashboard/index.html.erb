<h2>Create a new list:</h2>


<%= render partial: 'lists/new', locals: { lists: current_user.lists.build } %>

<ul class="lists">
  <%= render partial: 'lists/list', locals: { list: current_user.lists } %>
</ul>
